thnings to fix:

1. deleting room -> should also delete all its appended sensors.
2. when sensor is de-activaed via sensorpage - it stills shows as active on the dashboard.


important features:

1. move from UI polling to websocket.
2. on light sensors - add time counter to "on" state.